# OpenAI
OPENAI_API_KEY=sk-...

# Upstash Redis (REST). Create a DB at https://console.upstash.com/redis
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxx

# Token to authorize POST /api/reset?token=...
RESET_TOKEN=change-me-to-something-long

# Public URL the QR code will encode. Locally: http://localhost:3000 (or ngrok URL).
# Prod: https://cursor-hack-bay.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000
